# Disabled: this workflow targeted a Prisma-based backend that no longer exists
# in this repo. The real SQL regression suite now runs via:
#   - .github/workflows/backend-sql-tests.yml          (dedicated, blocking)
#   - .github/workflows/production-deploy.yml :: backend-tests job
#
# Kept as a stub so external CI references don't 404.
name: Test Suite (legacy — disabled)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy Prisma-based test suite has been retired. See backend-sql-tests.yml."
