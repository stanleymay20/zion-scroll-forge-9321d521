import { canonicalLifecycleRoutes } from "./studentLifecycle";

/** Backwards-compatible export; canonical route definitions live in studentLifecycle.ts. */
export const onboardingRoutes = canonicalLifecycleRoutes;
